---
plan: seo
seq: 001
stage: done
ctx_pct_at_checkpoint: 55
prev: v041-content-refresh-0001
eval: L1
---
## Done
- Added the SEO surface the site lacked (Vercel SEO starter + Next 16 metadata
  conventions; verified against bundled `node_modules/next/dist/docs`). Filed
  issues #13–#15 first, then one PR (#16, Closes #13–#15):
  - **#13** `app/sitemap.ts` (derived from `DOCS_NAV`) + `app/robots.ts` →
    `/sitemap.xml` + `/robots.txt` static routes.
  - **#14** `alternates.canonical`: default "/" in `layout.tsx` + per-route on all
    four docs pages (a child metadata omitting `alternates` would inherit "/").
  - **#15** `JsonLd` component + `lib/structured-data.ts`: `WebSite` +
    `Organization` site-wide, `SoftwareApplication` homepage-only.
  - Metadata polish: `applicationName`/`creator`/`publisher`.
  - Clean-code follow-through: centralized the description as
    `SITE_DESCRIPTION` in `site.ts` (the one DRY smell the L1 reviewer flagged).
- Out of scope (delete before you add): PWA web manifest (needs new icon assets).
- Verified: lint + build green; `/sitemap.xml` (5 URLs) + `/robots.txt` correct;
  per-route canonicals + valid JSON-LD in prerendered HTML. Independent **L1**
  reviewer PASSed 4/4. Plan archived to `completed/0008-seo.md`.

## Now
- `seo` DONE pending merge of PR #16. Website last released = v0.2.4.

## Next (resume here)
- Merge PR #16 → tag the website `v0.3.0` (a feature → MINOR bump) to ship.
- Post-deploy: validate `/robots.txt` + `/sitemap.xml` are 200 in prod; optionally
  submit the sitemap in Google Search Console and check rich-results for the
  SoftwareApplication JSON-LD.

## Decisions
- Sitemap derived from `DOCS_NAV` (single source; new docs route → auto-updates).
- `SoftwareApplication` (DeveloperApplication, free Offer, codeRepository, MIT) is
  the most rich-result-friendly schema type for an open-source CLI tool.
- Skipped the web manifest deliberately (SEO ≠ PWA; avoid new icon assets).
- `eval: L1` — additive, standard Next conventions (not architecture/security).

## Open questions / blockers
- none.
