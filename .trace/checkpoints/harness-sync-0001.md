---
plan: harness-sync
seq: 001
stage: done
ctx_pct_at_checkpoint: 35
prev: release-0001
---
## Done
- Surfaced the documented harness-mini version on the site + built a drift
  detector so the hand-curated mirror stays honest as upstream iterates.
- `site/src/lib/site.ts`: new `HARNESS_RELEASE` (`tag`/`date`/`url` w/
  `x-release-*` bot markers + human `headline`), derived `HARNESS_VERSION`,
  `CHANGELOG_URL`. **Fixed real drift:** added the `release` skill that upstream
  v0.1.0 shipped but the site was missing (SKILLS 14 → 15).
- Display: hero `<Eyebrow>` version pill, footer "documenting v0.1.0", and a
  "What's new in v0.1.0 → Changelog" callout under the install terminal. All
  three render in the prerendered HTML, linked to the release.
- `bin/sync-harness.mjs` (zero-dep Node): diffs upstream latest tag + `skills/`
  + `agents/` inventory + CHANGELOG vs `site.ts`; auto-bumps the version lines;
  flags content drift (never writes prose); writes `harness/upstream-sync.json`
  provenance + a PR-body report. `--check` = report-only, exit 1 on drift.
- `.github/workflows/sync-harness-version.yml`: daily `schedule` +
  `workflow_dispatch`; on drift runs lint+build then opens a PR as
  `github-actions[bot]` (peter-evans/create-pull-request@v7), gated on
  `steps.sync.outputs.drift`.
- Docs: README "Staying current", CONTRIBUTING triage note, ARCHITECTURE.
  `.gitignore` ignores the transient `.sync-report.md`.
- Verified: lint clean, build green (10 static routes), `--check` idempotent
  (exit 0) + synthetic-drift exit 1 then restored, YAML valid. Independent
  evaluator PASSed **6/6** with evidence; plan archived to
  `completed/0005-harness-sync.md`.

## Now
- `harness-sync` DONE. No active plans. Latest documented tag = `v0.1.0`.
  Nothing committed yet — changes are in the working tree (see `git status`).

## Next (resume here)
- Commit the work on a branch + open a PR (CI gate), then cut a release tag to
  ship to production (`main` is preview-only).
- Add the `SYNC_PAT` secret if you want the bot's PR to also trigger `ci.yml`
  (today the sync job runs lint+build itself to cover that).
- Possible follow-on (own plan): a `/changelog` route, or generating
  SKILLS/AGENTS from upstream frontmatter once that schema is pinned.

## Decisions
- Drift = **detect + itemize** (user's call); prose stays human-curated, only
  the version auto-bumps. `site.ts` is the single inventory SoT (diff against it
  directly — names only, blurbs aren't flagged).
- Provenance = `{tag, commit, syncedAt}` — no upstream `harness.lock` is
  committed (it's generated by `init.sh`), so commit-SHA is the staleness key.
- `commits-ahead` is reported but does NOT trigger a PR (only version/inventory
  do) — keeps the bot quiet on upstream changes that don't touch the catalog.

## Open questions / blockers
- none.
