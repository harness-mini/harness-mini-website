---
plan: ship
seq: 001
stage: done
ctx_pct_at_checkpoint: 38
prev: website-0002
---
## Done
- **og-image** slice: `src/app/opengraph-image.tsx` (next/og ImageResponse,
  1200×630, terminal aesthetic, counts derived from `SKILLS`/`AGENTS`) +
  `twitter-image.tsx` (re-export). `og:image*`/`twitter:image*` + alt verified.
- **deploy** slice: pushed to GitHub `harness-mini/harness-mini-website`
  (public); Vercel project `atlanticfs-projects/harness-mini-website`,
  Root Directory = `site/` (set via Vercel API). Published with `vercel --prod`
  from the repo root → live: https://harness-mini-website.vercel.app
- `SITE_URL` wired into `metadataBase` + `openGraph.url` + a footer "Live site"
  link; OG image URLs resolve to the live domain.
- Independent `evaluator` (fresh, opus) PASSed 4/4 by live HTTP + byte evidence.
- Stage advanced `evaluate → done`; plan archived to `completed/0002-ship.md`.

## Now
- `ship` is DONE. No active plans. Site is live and serving 200 on all routes.

## Next (resume here — would be a NEW plan)
- **Enable auto-deploy on push:** authorize the Vercel GitHub app on the
  `harness-mini` org (install https://github.com/apps/vercel with access to
  `harness-mini-website`), then `cd /Users/matt/AI/harness-mini-website &&
  vercel git connect`. Until then, publish via `vercel --prod` from the repo
  root (Root Directory = `site/` is already set).
- Optional: custom domain.

## Decisions
- Deploy from the repo ROOT (not `site/`) so CLI builds and future git builds
  both honor Root Directory = `site/`. The nested create-next-app git repo was
  dissolved so the project is a single repo at root.

## Open questions / blockers
- Auto-deploy blocked only on the Vercel↔GitHub-app authorization (browser,
  org admin). Not an acceptance criterion; publishing works via CLI.
